apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: trainer-builder-webhook
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    trainer-builder-webhook:
      port: "12000"
      endpoint: /trainer
      method: POST
