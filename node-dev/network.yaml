apiVersion: v1
kind: Service
metadata:
  name: node-dev
spec:
  selector:
    app: node-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: node-dev-payload
spec:
  selector:
    app: node-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4200
      name: http     name: http
---
# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: node-dev
# spec:
#   podSelector:
#     matchLabels:
#       app: node-dev
#   policyTypes:
#     - Ingress
#     - Egress
#   ingress:
#     - from:
#         - podSelector:
#             matchLabels:
#               app: node-dev-proxy
#       ports:
#         - protocol: TCP
#           port: 8080
#   egress:
#     - to:
#         - ipBlock:
#             cidr: 0.0.0.0/0
#             except:
#               - 10.0.0.0/8
#               - 192.168.0.0/16
#       ports:
#         - protocol: TCP
#           port: 443
#     - to:
#         - namespaceSelector:
#             matchLabels:
#               kubernetes.io/metadata.name: kube-system
#       ports:
#         - protocol: UDP
#           port: 53
